source 'git@code.byted.org:TTIOS/tt_pods_specs.git'
source 'git@code.byted.org:TTIOS/cocoapods-master-specs-mirror.git'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TTUGCFoundation' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for TTUGCFoundation
  # pod 'TTUGCFoundation/TTUGCRichText', :path => '../', :inhibit_warnings => false
  # pod 'TTUGCFoundation/TTUGCComment', :path => '../', :inhibit_warnings => false
  # pod 'TTTracker', '1.0.0.6'
  # pod 'TTInstallService', '1.0.0.6'
  # pod 'libextobjc', '0.4.1'
  # pod 'TTComment', :path => '../../../../tt_pods_comment/'
  # pod 'TTUGCFoundation/TTUGCActionDataService', :path => '../', :inhibit_warnings => false
  pod 'TTUGCFoundation/TTKitchen', :path => '../', :inhibit_warnings => false
  #-----账号登录UI库-----
  # pod 'TTAccountLogin', '1.4.4'
  #-----账号的业务库----
  # pod 'TTNewsAccountBusiness', '0.2.0'
  #-----TTAccountSDK-----
  # pod 'TTAccountSDK', '2.4.3', :subspecs => [
  # 'Account',
  # 'WeChatAccount',
  # 'SinaWeiboAccount',
  # 'TencentQQAccount',
  # 'TencentWBAccount',
  # 'RenRenAccount',
  # 'TianYiAccount',
  # 'TTAwemeAccount',
  # 'TTHotsoonAccount'
  # ]
end
